name: CI

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: nixos
    steps:
      - uses: actions/checkout@v6
      - uses: cachix/cachix-action@v15
        with:
          name: paolino
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - name: Build executable
        run: nix --quiet build .#mts
      - name: Build docker image
        run: nix --quiet build .#docker-image -o mts-image
      - name: Upload Artifact
        uses: actions/upload-artifact@v6
        with:
          name: mts-image
          path: mts-image
      - name: Build appImage artifact
        run: nix --quiet bundle --bundler github:ralismark/nix-appimage .#mts -o mts.AppImage
      - name: Upload appImage artifact
        uses: actions/upload-artifact@v6
        with:
          name: mts-appImage
          path: mts.AppImage
      - name: Build rpm artifact
        run: nix --quiet bundle --bundler github:NixOS/bundlers#toRPM -o mts.rpm
      - name: Upload rpm artifact
        uses: actions/upload-artifact@v6
        with:
          name: mts-rpm
          path: mts.rpm
      - name: Build deb artifact
        run: nix --quiet bundle --bundler github:NixOS/bundlers#toDEB -o mts.deb
      - name: Upload deb artifact
        uses: actions/upload-artifact@v6
        with:
          name: mts-deb
          path: mts.deb
  unit:
    name: Run unit Tests
    runs-on: nixos
    steps:
      - uses: actions/checkout@v6
      - uses: cachix/cachix-action@v15
        with:
          name: paolino
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - name: Run Tests
        run: nix --quiet run .#unit-tests
  bench:
    name: Run Benchmarks
    runs-on: nixos
    steps:
      - uses: actions/checkout@v6
      - uses: cachix/cachix-action@v15
        with:
          name: paolino
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - name: Run Benchmarks
        run: nix --quiet run .#bench
      - name: Run Populate Benchmarks
        run: nix --quiet run .#populate-bench -- 1000 5000
  typescript:
    name: TypeScript Verifier Tests
    runs-on: nixos
    steps:
      - uses: actions/checkout@v6
      - uses: cachix/cachix-action@v15
        with:
          name: paolino
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - name: Run TypeScript Tests
        run: nix --quiet run ./verifiers/typescript#test
  formatting:
    name: Check code formatting
    runs-on: nixos
    steps:
      - uses: actions/checkout@v6
      - uses: cachix/cachix-action@v15
        with:
          name: paolino
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - name: Check Formatting
        run: nix --quiet develop -c just format-check
  cabal-check:
    name: Cabal check
    runs-on: nixos
    steps:
      - uses: actions/checkout@v6
      - uses: cachix/cachix-action@v15
        with:
          name: paolino
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - name: Run cabal check
        run: nix --quiet develop -c cabal check
  hlint:
    name: HLint
    runs-on: nixos
    steps:
      - uses: actions/checkout@v6
      - uses: cachix/cachix-action@v15
        with:
          name: paolino
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - name: Run HLint
        run: nix --quiet develop -c just hlint
  lean:
    name: Lean proofs
    runs-on: nixos
    steps:
      - uses: actions/checkout@v6
      - name: Build Lean proofs
        working-directory: lean
        run: nix --quiet shell nixpkgs#elan -c lake build
        env:
          LEAN_OPTS: "-DwarningAsError=true"
      - name: Check for sorry
        run: |
          if grep -rn 'sorry' --include='*.lean' lean/Rollbacks/ lean/Rollbacks.lean; then
            echo "FAIL: sorry found in proofs"
            exit 1
          fi

  version-sync:
    name: Cabal version matches manifest
    runs-on: nixos
    steps:
      - uses: actions/checkout@v6
      - name: Assert mts.cabal version equals release manifest
        run: |
          man=$(sed -n 's/.*"\.":[[:space:]]*"\([^"]*\)".*/\1/p' .release-please-manifest.json)
          cab=$(sed -n 's/^version:[[:space:]]*\([0-9][0-9.]*\).*/\1/p' mts.cabal | head -1)
          echo "manifest=$man cabal=$cab"
          if [ "$cab" != "$man" ] && [ "$cab" != "${man}.0" ]; then
            echo "::error::mts.cabal version ($cab) does not match release manifest ($man); release-please owns this — see release-please-config.json extra-files"
            exit 1
          fi
